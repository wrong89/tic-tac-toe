import Cell from '../Cell/Cell';
import Cells from '../Cells/Cells';

const Board = () => {
    return (
        <div>
            <h1>Hello board</h1>
            <Cell />
            <Cells />
        </div>
    );
};

export default Board;
